-- Stack machine.

import Array;
import List;
import Fun;
import Collection;
import World;
import State;
import Expr;
import Buffer;
import Fun;

-- Stack code printer. Takes a list of SM instructions, return its
-- string representation.
fun myReverse2(code,tw){
	case code of
		[]->tw
		|x:y->myReverse2(y,x:tw)
	esac
}
fun myReverse(code){
	myReverse2(code,[])
}
public fun showSMInsn (i) {
  case i of
    READ         -> "READ"
  | WRITE        -> "WRITE"
  | BINOP (s)    -> sprintf ("BINOP %s", s)
  | LD    (x)    -> sprintf ("LD %s", x)
  | LDA   (x)    -> sprintf ("LDA %s", x)
  | ST    (x)    -> sprintf ("ST %s", x)
  | STI          -> "STI"
  | CONST (n)    -> sprintf ("CONST %d", n)
  | LABEL (s)    -> sprintf ("LABEL %s", s)
  | JMP   (l)    -> sprintf ("JMP %s", l)
  | CJMP  (c, l) -> sprintf ("CJMP %s, %s", c, l)
  | DUP          -> "DUP"
  | DROP         -> "DROP"  
  esac
}

public fun showSM (prg) {
	case prg of
		[]->""
		|x:y->showSM(y)++showSMInsn(x)++"\n"
	esac
}

-- Evaluation environment: keeps a mapping between labels and (sub)programs
fun emptyEnvF (x) {
  failure ("undefined label ""%s""\n", x)
}
fun initEvalEnv (insns) {
	var f=emptyEnvF;
	var running=1;
	while running do
		case insns of
			[]->(running:=0)
			|x:y->
				case x of
					LABEL(lab) -> (f:=(f<-[lab,y]))
					|_->skip
				esac;
			insns:=y
		esac
	od;
	[f]
}

-- Accessor function
fun fromLabel (env, lab) {
  env [0] (lab)
}

-- Stack machine interpreter. Takes an environment, an SM-configuration and a program,
-- returns a final configuration
fun eval (env, [stack,state,[i,o]], insns) {
	var debug=0;
	var running=1;
	--failure(showSM(insns));
	--printf(insns.string);
	--printf("\n");
	--write(state("res"));
	--printf("\n");
	--write(state("n"));
	--printf("\n");
	--write(state("k"));
	--printf("\n");
	while running do
		case insns of
			[]->(running:=0)
			|x:y-> case x of
				READ->case i of
					f:r->
						if debug then printf("READING\n") else Nop fi;
						stack:=(f:stack);
						i:=r;
						insns:=y
					esac
				|WRITE->case stack of
					f:r->
						if debug then printf("WRITING\n") else Nop fi;
						stack:=r;
						o:=(f:o);
						insns:=y
					esac
				|LD(v)->
					if debug then printf("LOADING %s\n",v) else Nop fi;
					stack:=(state(v):stack);
					insns:=y
				|LDA(v)->
					if debug then printf("LDA %s\n",v) else Nop fi;
					stack:=(v:stack);
					insns:=y
				|ST(v)->
					if debug then printf("STORING %s\n",v) else Nop fi;
					case stack of
						f:r->
							stack:=r;
							state:=state<-[v,f];
							insns:=y
					esac
				|STI->
					if debug then printf("STI\n") else Nop fi;
					case stack of
						f:v:r->
							stack:=f:r;
							state:=state<-[v,f];
							insns:=y
					esac
				|DUP->
					if debug then printf("DUP\n") else Nop fi;
					case stack of
						f:r->
							stack:=f:f:r;
							insns:=y
					esac
				|DROP->
					if debug then printf("DROP\n") else Nop fi;
					case stack of
						f:r->
							stack:=r;
							insns:=y
					esac
				|CONST(n)->
					if debug then printf("CONSTING %i\n",n) else Nop fi;
					stack:=(n:stack);
					insns:=y
				|BINOP(a)->
					if debug then printf("BINOPING %s\n",a) else Nop fi;
					case stack of
						s:f:r->
							stack:=(evalOp(a,f,s):r);
							insns:=y
					esac
				|JMP(l)->
					if debug then printf("JUMPING %s\n",l) else Nop fi;
					insns:=env.fromLabel(l)
				|CJMP("nz",l)->
					if debug then printf("CHECKING nz\n") else Nop fi;
					case stack of
					f:r->
						if f
						then
							if debug then printf("JUMPING %s\n",l) else Nop fi;
							insns:=env.fromLabel(l)
						else
							if debug then printf("NOTJUMPING %s\n",l) else Nop fi;
							insns:=y
						fi
					esac
				|CJMP("z",l)->
					if debug then printf("CHECKING z\n") else Nop fi;
					case stack of
					f:r->
						stack:=r;
						if f
						then
							if debug then printf("NOTJUMPING %s\n",l) else Nop fi;
							insns:=y
						else
							if debug then printf("JUMPING %s\n",l) else Nop fi;
							insns:=env.fromLabel(l)
						fi
					esac
				|LABEL(l)->
					if debug then printf("MARKING %s\n",l) else Nop fi;
					insns:=y
			esac
		esac
	od;
	[stack,state,[i,o]]
}

-- Runs a stack machine for a given input and a given program, returns an output
public fun evalSM (input, insns) {
	var r=myReverse(insns);
	var env=initEvalEnv (r);
	--printf("PREEVAL\n");
	case eval (env, [{}, emptyState, createWorld (input)], r) of [stack,state,[i,o]] ->
		--printf("POSTEVAL\n");
		reverse(o)
	esac
}

-- Compilation environment: generates labels
fun makeCompEnv (n) {
  [fun () {
     [sprintf ("L%d", n), makeCompEnv (n+1)]
  }]
}

fun initCompEnv () {
  makeCompEnv (0)
}

-- Accessor function: generates one label
fun genLabel (env) {
  env [0] ()
}

-- Utility function: generates n labels
fun genLabels (env, n) {
  fun inner (env, n) {
    if n == 0
    then {env}
    else case env.genLabel of
           [lab, env] -> lab : inner (env, n-1)
         esac
    fi
  }

  listArray (inner (env, n))
}

-- Compiles an expression into a stack machine code.
-- Takes an expression, returns a list of stack machine
fun merge(a,b){
	case a of
		[]->b
		|x:y->x:merge(y,b)
	esac
}
-- instructions.
public fun compileSM (stmt) {
	fun doCompile (lab, env, stmt,tw) {
		--printf("%s\n",stmt.string);
		case stmt of
			Skip->[false,env,tw]
			|Var(x)->[false,env,LD(x):tw]
			|Ref(x)->[false,env,LDA(x):tw]
			|Const(n)->[false,env,CONST(n):tw]
			|Binop(a,b,c)->case doCompile(lab,env,Seq(Seq(b,c),Skip),tw) of [false,env,code]->
					[false,env,BINOP(a):code]
				esac
			|Read(x)->case doCompile(lab,env,Seq(x,Skip),tw) of [false,env,code]->
					[false,env,DROP:STI:READ:code]
				esac
			|Ignore(x)->case doCompile(lab,env,Seq(x,Skip),tw) of [false,env,code]->
					[false,env,DROP:code]
				esac
			|Write(e)->case doCompile(lab,env,Seq(e,Skip),tw) of [false,env,code]->
					[false,env,WRITE:code]
				esac
			|Assn(x,e)->case doCompile(lab,env,Seq(Seq(x,e),Skip),tw) of [false,env,code]->
					[false,env,STI:code]
				esac
			|Seq(s1,s2)->
				case env.genLabel of [s2Lab,env]->
					case doCompile(s2Lab,env,s1,tw) of [s2LabUsed,env,s1Code]->
						if s2LabUsed then
							doCompile(lab,env,s2,LABEL(s2Lab):s1Code)
						else
							doCompile(lab,env,s2,s1Code)
						fi
					esac
				esac
			|While(a,b)->
				case env.genLabel of [start,env] ->
					case doCompile(lab,env,Seq(a,Skip),LABEL(start):tw) of [false,env,code]->
					case doCompile(start,env,b,CJMP("z",lab):code) of [_, env, code]->
						[true,env,JMP(start):code]
					esac
					esac
				esac
			|If(a,b,c)->
				case env.genLabel of [ifbad,env]->
					case doCompile(lab,env,Seq(a,Skip),tw) of [false,env,code]->
					case doCompile(lab,env,b,CJMP("z",ifbad):code) of [needB,env,codeB]->
					case doCompile(lab,env,c,LABEL(ifbad):JMP(lab):codeB) of [_,env,codeC]->
						[true,env,codeC]
					esac
					esac
					esac
				esac
			|DoWhile(a,b)->
				case env.genLabel of [start,env]->
				case env.genLabel of [end,env]->
				case doCompile(end,env,a,LABEL(start):tw) of
					[true,env,code]->
							case doCompile(lab,env,b,LABEL(end):code) of [false,env,code]->
								[false,env,CJMP("nz",start):code]
							esac
					|[false,env,code]->
							case doCompile(lab,env,b,code) of [false,env,code]->
								[false,env,CJMP("nz",start):code]
							esac
				esac
				esac
				esac
		esac
	}
	fun compile (lab, env, stmt) {
		doCompile(lab,env,stmt,[])
	}
	--stmt:=Read(Ref("x"));
	case initCompEnv ().genLabel of [endLab, env] ->
		case compile (endLab, env, stmt) of [endLabUsed, _, code] ->
			if endLabUsed
				then LABEL(endLab):code
				else code
			fi
		esac
	esac
}
