import List;
import Array;
import World;
import Stmt;
import State;
import SM;
import Embedding;

case evalStmt ([emptyState, createWorld (map (stringInt, arrayList (sysargs).tl))],
               program ()) of
 [_, w] -> iter (write, getOutput (w))
esac