-- Parser 

import Ostap;
import Lexer;
import List;
import Fun;
import Matcher;

-- A parser of "something" in brackets; l, r are left and right
-- brackets as parsers, p --- a parser of "something"
fun inbr (l, p, r) {
  syntax (-l p -r)
}

-- Primary expression
var primary = memo $ eta syntax (
        x=decimal {Const (stringInt (x))} |
        x=lident  {Var (x)}               |
        inbr[s("("), exp, s(")")]
      ),
      exp = memo $ eta (expr ({
        [Left, {
          [s("!!"), fun(l, op, r) { Binop(op, l, r) }]
        }],
        [Left, {
          [s("&&"), fun(l, op, r) { Binop(op, l, r) }]
        }],
        [Nona, {
          [s("=="), fun(l, op, r) { Binop(op, l, r) }],
          [s("!="), fun(l, op, r) { Binop(op, l, r) }],
          [s("<"), fun(l, op, r) { Binop(op, l, r) }],
          [s("<="), fun(l, op, r) { Binop(op, l, r) }],
          [s(">"), fun(l, op, r) { Binop(op, l, r) }],
          [s(">="), fun(l, op, r) { Binop(op, l, r) }]
        }],
        [Left, {
          [s("+"), fun(l, op, r) { Binop(op, l, r) }],
          [s("-"), fun(l, op, r) { Binop(op, l, r) }]
        }],
        [Left, {
          [s("*"), fun(l, op, r) { Binop(op, l, r) }],
          [s("/"), fun(l, op, r) { Binop(op, l, r) }],
          [s("%"), fun(l, op, r) { Binop(op, l, r) }]
        }]
      }, primary));

var ifParser = memo $ eta syntax (
  kElif e=exp kThen s=stmt s2=ifParser { If(e, s, s2) } |
  kElse s=stmt kFi { s } |
  kFi { Skip }
);

var stmt2 = memo $ eta syntax (
  x=lident s[":="] y=exp
    { Assn(x, y) } |
  kRead x=inbr[s("("), lident, s(")")]
    { Read(x) } |
  kWrite x=inbr[s("("), exp, s(")")]
    { Write(x) } |
  kSkip
    { Skip } |
  kIf e=exp kThen s=stmt kFi
    { If(e, s, Skip) } |
  kIf e=exp kThen s1=stmt s2=ifParser
    { If(e, s1, s2) } |
  kWhile e=exp kDo s=stmt kOd
    { While(e, s) } |
  kDo s=stmt kWhile e=exp kOd
    { DoWhile(e, s) } |
  kFor start=stmt s[","] cond=exp s[","] end=stmt kDo body=stmt kOd
    { Seq(start, While(cond, Seq(body, end))) }
);

var stmt = memo $ expr({[Left, {[s(";"), fun (l, op, r) {
  -- printf("%s %s\n", string(l), string(r));
  Seq(l, r)
}]}]}, stmt2);
   

-- Public top-level parser
public parse = stmt;
